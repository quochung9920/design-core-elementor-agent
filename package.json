{
  "name": "design-core-elementor-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check scripts/browser-probe.mjs && node --check scripts/browser-analyze.mjs && node --check scripts/browser-analyze-target.mjs && node --check scripts/capture-page.mjs && node --check scripts/visual-compare.mjs && node --check scripts/qa-air-page.mjs"
  },
  "devDependencies": {
    "pixelmatch": "^7.1.0",
    "playwright": "^1.55.0",
    "pngjs": "^7.0.0"
  }
}
