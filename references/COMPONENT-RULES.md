# Component Rules

- Reuse existing registry items before creating new ones.
- Prefer semantic variants over separate duplicate components.
- Store component metadata in a persistent registry.
- Create a custom widget only for reusable behavior or complex domain logic.
- Do not duplicate components solely because of small UI differences.
