{
  "name": "design-core-mcp",
  "private": true,
  "version": "1.0.0-rc21",
  "description": "rc21 MCP Bridge: adapter between ChatGPT (Model Context Protocol) and the Design Core Elementor REST v2 API. Contains no Elementor mapping, Design IR planning, section intelligence or visual correction logic of its own -- every tool call is a schema-validated pass-through to Design Core.",
  "type": "module",
  "main": "dist/src/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "npm run build && node --test dist/test/**/*.js",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "express": "5.2.1",
    "express-rate-limit": "8.7.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/express": "5.0.6",
    "@types/node": "26.4.1",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
